{
  "name": "single-skill-template",
  "version": "0.1.0",
  "private": true,
  "description": "Production-ready template for one focused agent skill repository.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-skill.mjs",
    "test": "node --test tests/*.test.mjs",
    "scaffold": "node scripts/scaffold-skill.mjs",
    "package": "node scripts/package-skill.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
